{
  "name": "fitzone-app",
  "version": "0.0.1",
  "private": true,
  "description": "FitZone mobile app (Capacitor shell wrapping the frontend-users web build)",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../frontend-users run dev",
    "copy:web": "node scripts/copy-web.mjs",
    "build:web": "npm --prefix ../frontend-users install && npm --prefix ../frontend-users run build",
    "build": "npm run build:web && npm run copy:web && cap sync",
    "sync": "npm run copy:web && cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "run:android": "npm run build && cap run android",
    "run:ios": "npm run build && cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "typescript": "~5.8.3"
  }
}
